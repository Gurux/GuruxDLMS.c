[package]
name = "dlms_cosem_rs"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
aes-gcm = { version = "0.10.3", default-features = false, features = ["aes", "alloc"] }
